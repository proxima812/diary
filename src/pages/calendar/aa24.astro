---
import ReflectionCalendar from "@/components/ReflectionCalendar.astro"
import { aa24hours } from "@/data/reflections/aa24hours"
import Layout from "@/layouts/Layout.astro"
import { buildAvailableSlugs } from "@/utils/calendar"

const availableSlugs = buildAvailableSlugs(aa24hours)
---

<Layout title="Календарь Я и АА 24 часа">
	<ReflectionCalendar basePath="aa24" availableSlugs={availableSlugs} />
</Layout>
