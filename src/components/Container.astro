---
import { cn } from "@/utils/cn"

interface Props {
	class?: string
}

const { class: className, ...props } = Astro.props
---

<div class={cn("container flex-1 px-5 mx-auto max-w-6xl", className)} {...props}>
	<slot />
</div>
